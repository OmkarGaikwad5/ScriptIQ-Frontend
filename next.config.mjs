/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
    domains: ['media2.dev.to', 'dev-to-uploads.s3.amazonaws.com','res.cloudinary.com'],
    
  },
};

export default nextConfig;
